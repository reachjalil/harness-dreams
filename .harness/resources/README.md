# Harness Resources

Shared skills, rules, and target-specific seeds for this repository's generated
agent surfaces. Edit these source files, then run `pnpm harness:preview` before
applying changes with `pnpm harness:activate`.

Machine-local settings, permissions, secrets, logs, and caches stay out of this
directory.
